<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Mises à jour automatiques In-App (www/js/maj-apk.js, distribution
         hors Play Store via GitHub Releases) : nécessaire pour qu'Android
         autorise le lancement de son installeur système sur l'APK
         téléchargé (voir ApkInstallerPlugin). Sur Android 8+, l'utilisateur
         doit en plus activer explicitement "Installer des applications
         inconnues" pour Logisol, ce réglage supplémentaire ne pouvant pas
         être accordé par cette permission seule (voir
         ApkInstallerPlugin.canRequestInstall/openInstallPermissionSettings). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Géolocalisation : utilisée par le bouton 📍 et par le centrage
         automatique au tout premier lancement (aucune parcelle enregistrée),
         via navigator.geolocation dans la WebView. Capacitor
         (BridgeWebChromeClient.onGeolocationPermissionsShowPrompt) demande
         l'autorisation à l'exécution, mais uniquement si elle est déclarée
         ici. Sans ces deux lignes, la demande échoue immédiatement et la
         carte reste sur la France. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
</manifest>
