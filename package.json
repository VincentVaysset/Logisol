{
  "name": "logisol",
  "version": "0.1.0",
  "private": true,
  "description": "Logisol — gestion parcellaire pour exploitation agricole (ovins laitiers)",
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
